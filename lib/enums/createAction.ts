export enum SpecialTableType {
    PRODUCT = "product",
    NOTIFICATION = "notification",
}

export enum ColumnTypeChoice {
    STRING = "string",
    BOOLEAN = "boolean",
    NUMBER = "number",
    ENUM = "enum",
    DATE = "date",
    TIME = "time",
    OBJECT = "object",
    ARRAY = "array",
    IMAGE = "image",
}
