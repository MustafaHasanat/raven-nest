version: "3.8"
services:

  postgres-db:
    container_name: postgres
    image: postgres
    restart: always
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data

  nest-app:
    container_name: rnk-system
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    environment: 
      - PORT=${PORT}
    ports:
      - "3000:3000"
    depends_on:
      - postgres-db
    volumes:
      - ./src:/app/src

  # pgadmin:
  #   container_name: pgadmin
  #   image: dpage/pgadmin4
  #   restart: always
  #   environment:
  #     PGADMIN_DEFAULT_EMAIL: arromi.creatives@gmail.com
  #     PGADMIN_DEFAULT_PASSWORD: 5^00583Arromi
  #   ports:
  #     - "5050:80"
  #   depends_on:
  #     - db